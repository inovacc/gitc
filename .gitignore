/target
Cargo.lock
desktop.ini
# git's own build output inside the vendored submodule (objects, archives,
# binaries). The SOURCE is tracked by the submodule; the artifacts are not.
vendor/git/**/*.o
vendor/git/**/*.a
vendor/git/**/*.exe
