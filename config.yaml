environment: dev
features:
  logging: true
  observability: false
logger:
  level: info
  format: json
  redact: true
observability:
  protocol: grpc
  sample_ratio: 1.0
# app-specific configuration:
greeting: hello
